services:
  masareef-api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: masareef-api
    restart: unless-stopped
    ports:
      - "8000:8000"
    networks:
      - masareef

  masareef-web:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: masareef-web
    restart: unless-stopped
    ports:
      - "3001:3000"
    depends_on:
      - masareef-api
    networks:
      - masareef

networks:
  masareef:
    driver: bridge
