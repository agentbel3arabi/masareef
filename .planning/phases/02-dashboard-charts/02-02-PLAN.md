---
phase: 02-dashboard-charts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/api-client.ts
  - frontend/src/hooks/use-dashboard.ts
  - frontend/src/hooks/use-households.ts
  - frontend/src/components/dashboard/chart-skeleton.tsx
  - frontend/src/components/dashboard/time-range-toggle.tsx
  - frontend/src/components/dashboard/compare-toggle.tsx
  - frontend/src/components/dashboard/base-currency-selector.tsx
  - frontend/src/components/dashboard/delta-summary-card.tsx
  - frontend/src/components/shared/stat-card.tsx
  - frontend/src/lib/types/dashboard.ts
  - frontend/messages/en.json
  - frontend/messages/ar.json
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-05
  - DASH-06

must_haves:
  truths:
    - "Dashboard TypeScript types match the backend Pydantic schemas exactly"
    - "TanStack Query hooks exist for all 4 dashboard endpoints with proper query keys"
    - "apiPatch function exists in api-client.ts for household settings update"
    - "Household hook includes mutation for updating base_currency"
    - "ChartSkeleton renders an animate-pulse placeholder at chart dimensions"
    - "TimeRangeToggle renders 1M/3M/6M/1Y/All buttons with correct active styling"
    - "BaseCurrencySelector reads from household data and writes via PATCH endpoint"
    - "StatCard supports percentChange in trend prop for percentage display"
    - "All new i18n keys exist in both en.json and ar.json"
  artifacts:
    - path: "frontend/src/lib/types/dashboard.ts"
      provides: "TypeScript interfaces for all dashboard API responses"
      exports: ["IncomeVsExpensesMonth", "SpendingByCategory", "NetWorthTrendPoint", "StatCardsData", "StatCardItem", "StatCardTrend"]
    - path: "frontend/src/hooks/use-dashboard.ts"
      provides: "TanStack Query hooks for dashboard endpoints"
      exports: ["useIncomeVsExpenses", "useSpendingByCategory", "useNetWorthTrend", "useStatCards"]
    - path: "frontend/src/components/dashboard/chart-skeleton.tsx"
      provides: "Loading skeleton for chart containers"
    - path: "frontend/src/components/dashboard/time-range-toggle.tsx"
      provides: "Segmented time range control"
    - path: "frontend/src/components/dashboard/compare-toggle.tsx"
      provides: "Compare toggle switch"
    - path: "frontend/src/components/dashboard/base-currency-selector.tsx"
      provides: "Currency selector dropdown"
    - path: "frontend/src/components/dashboard/delta-summary-card.tsx"
      provides: "Comparison delta card"
  key_links:
    - from: "frontend/src/hooks/use-dashboard.ts"
      to: "frontend/src/lib/api-client.ts"
      via: "apiGet calls to /api/v1/dashboard/*"
      pattern: "apiGet.*dashboard"
    - from: "frontend/src/components/dashboard/base-currency-selector.tsx"
      to: "frontend/src/hooks/use-households.ts"
      via: "useUpdateHouseholdSettings mutation"
      pattern: "useUpdateHouseholdSettings"
---

<objective>
Install frontend dependencies (react-plotly.js, shadcn skeleton/toggle), create dashboard TypeScript types, TanStack Query hooks, and all supporting UI components (skeleton, toggles, selectors, delta card). Enhance StatCard with percentage trend display.

Purpose: Prepare all frontend building blocks so Plan 03 can assemble them into the dashboard page without needing to create any primitives.
Output: Types, hooks, 5 new components, enhanced StatCard, i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-charts/02-CONTEXT.md
@.planning/phases/02-dashboard-charts/02-RESEARCH.md
@.planning/phases/02-dashboard-charts/02-UI-SPEC.md

@frontend/src/lib/api-client.ts
@frontend/src/hooks/use-transaction-summary.ts
@frontend/src/hooks/use-accounts.ts
@frontend/src/hooks/use-households.ts
@frontend/src/components/shared/stat-card.tsx
@frontend/src/components/dashboard/month-activity.tsx
@frontend/src/lib/money.ts
@frontend/src/lib/types/entities.ts
@frontend/messages/en.json
@frontend/messages/ar.json
@frontend/src/app/globals.css

<interfaces>
<!-- Key existing patterns the executor must follow -->

From frontend/src/lib/api-client.ts:
```typescript
export async function apiGet<T>(path: string): Promise<ApiResponse<T>>
export async function apiPost<T>(path: string, body: unknown): Promise<ApiResponse<T>>
export async function apiPut<T>(path: string, body: unknown): Promise<ApiResponse<T>>
// NOTE: apiPatch does NOT exist yet — Task 1 adds it
```

From frontend/src/hooks/use-transaction-summary.ts (pattern to follow):
```typescript
export function useTransactionSummary(params: SummaryParams = {}) {
  const searchParams = new URLSearchParams();
  // ... build query string from params
  return useQuery({
    queryKey: ["transaction-summary", params],
    queryFn: () => apiGet<TransactionSummary>(`/api/v1/transactions/summary${qs ? `?${qs}` : ""}`),
  });
}
```

From frontend/src/hooks/use-accounts.ts (mutation pattern):
```typescript
export function useCreateAccount() {
  const queryClient = useQueryClient();
  return useApiMutation({
    mutationFn: (data) => apiPost<Account>("/api/v1/accounts", data),
    onSuccess: () => { queryClient.invalidateQueries({ queryKey: ["accounts"] }); },
  });
}
```

From frontend/src/components/shared/stat-card.tsx:
```typescript
interface StatCardProps {
  icon: LucideIcon;
  label: string;
  value: string;
  trend?: { direction: "up" | "down" | "flat"; text: string; };
  variant?: "default" | "accent" | "success" | "destructive";
  className?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies + types + apiPatch + dashboard hooks + household mutation</name>
  <files>frontend/src/lib/api-client.ts, frontend/src/lib/types/dashboard.ts, frontend/src/hooks/use-dashboard.ts, frontend/src/hooks/use-households.ts</files>
  <read_first>
    - frontend/package.json
    - frontend/src/lib/api-client.ts
    - frontend/src/hooks/use-transaction-summary.ts
    - frontend/src/hooks/use-accounts.ts
    - frontend/src/hooks/use-households.ts
    - frontend/src/hooks/use-api-mutation.ts
    - frontend/src/lib/types/entities.ts
  </read_first>
  <action>
**Step 1: Install npm dependencies**
```bash
cd frontend
pnpm add react-plotly.js plotly.js-dist-min
pnpm add -D @types/react-plotly.js
pnpm dlx shadcn@latest add -y skeleton
pnpm dlx shadcn@latest add -y toggle
```
After shadcn install, audit `frontend/src/components/ui/skeleton.tsx` and `frontend/src/components/ui/toggle.tsx` for physical directional CSS classes (`pl-`, `pr-`, `left-`, `right-`, `ml-`, `mr-`) and convert to logical equivalents (`ps-`, `pe-`, `start-`, `end-`, `ms-`, `me-`).

**Step 2: Add `apiPatch` to `frontend/src/lib/api-client.ts`**
Add this function after the existing `apiPut` function:
```typescript
export async function apiPatch<T>(path: string, body: unknown): Promise<ApiResponse<T>> {
  const headers = await getAuthHeaders();
  const res = await fetch(`${API_BASE}${path}`, {
    method: "PATCH",
    headers,
    body: JSON.stringify(body),
  });
  if (!res.ok) await handleError(res);
  return res.json();
}
```

**Step 3: Create `frontend/src/lib/types/dashboard.ts`**
These types mirror the backend Pydantic schemas from `backend/app/schemas/dashboard.py`:
```typescript
export interface StatCardTrend {
  direction: "up" | "down" | "flat";
  absolute_delta: number;  // minor units
  percentage: number | null;  // null when previous period was 0
}

export interface StatCardItem {
  label: string;
  value_minor: number;
  currency: string;
  trend: StatCardTrend | null;
  count: number | null;  // for "Active Debts" card
}

export interface StatCardsData {
  net_worth: StatCardItem;
  spending: StatCardItem;
  active_debts: StatCardItem;
  upcoming_payments: StatCardItem;
}

export interface IncomeVsExpensesMonth {
  month: string;  // "2026-01"
  income_minor: number;
  expenses_minor: number;
  currency: string;
}

export interface SpendingByCategory {
  category_id: number | null;  // null for "Other"
  category_name: string;
  category_name_ar: string | null;
  category_color: string | null;
  amount_minor: number;  // absolute positive
  percentage: number;  // 0-100
  currency: string;
}

export interface NetWorthTrendPoint {
  month: string;  // "2026-01"
  accounts_minor: number;
  debts_minor: number;  // positive = debt owed
  net_worth_minor: number;  // accounts - debts
  currency: string;
}
```

**Step 4: Create `frontend/src/hooks/use-dashboard.ts`**
Follow the `use-transaction-summary.ts` pattern:
```typescript
"use client";

import { useQuery } from "@tanstack/react-query";
import { apiGet } from "@/lib/api-client";
import type {
  IncomeVsExpensesMonth,
  SpendingByCategory,
  NetWorthTrendPoint,
  StatCardsData,
} from "@/lib/types/dashboard";

export function useIncomeVsExpenses(params: { months?: number; base_currency?: string } = {}) {
  const searchParams = new URLSearchParams();
  if (params.months) searchParams.set("months", String(params.months));
  if (params.base_currency) searchParams.set("base_currency", params.base_currency);
  const qs = searchParams.toString();

  return useQuery({
    queryKey: ["dashboard", "income-vs-expenses", params],
    queryFn: () =>
      apiGet<IncomeVsExpensesMonth[]>(
        `/api/v1/dashboard/income-vs-expenses${qs ? `?${qs}` : ""}`
      ),
    staleTime: 60_000,
  });
}

export function useSpendingByCategory(params: { base_currency?: string } = {}) {
  const searchParams = new URLSearchParams();
  if (params.base_currency) searchParams.set("base_currency", params.base_currency);
  const qs = searchParams.toString();

  return useQuery({
    queryKey: ["dashboard", "spending-by-category", params],
    queryFn: () =>
      apiGet<SpendingByCategory[]>(
        `/api/v1/dashboard/spending-by-category${qs ? `?${qs}` : ""}`
      ),
    staleTime: 60_000,
  });
}

export function useNetWorthTrend(params: { months?: number; base_currency?: string } = {}) {
  const searchParams = new URLSearchParams();
  if (params.months) searchParams.set("months", String(params.months));
  if (params.base_currency) searchParams.set("base_currency", params.base_currency);
  const qs = searchParams.toString();

  return useQuery({
    queryKey: ["dashboard", "net-worth-trend", params],
    queryFn: () =>
      apiGet<NetWorthTrendPoint[]>(
        `/api/v1/dashboard/net-worth-trend${qs ? `?${qs}` : ""}`
      ),
    staleTime: 60_000,
  });
}

export function useStatCards(params: { base_currency?: string } = {}) {
  const searchParams = new URLSearchParams();
  if (params.base_currency) searchParams.set("base_currency", params.base_currency);
  const qs = searchParams.toString();

  return useQuery({
    queryKey: ["dashboard", "stat-cards", params],
    queryFn: () =>
      apiGet<StatCardsData>(
        `/api/v1/dashboard/stat-cards${qs ? `?${qs}` : ""}`
      ),
    staleTime: 60_000,
  });
}
```

**Step 5: Add household update mutation to `frontend/src/hooks/use-households.ts`**
Import `apiPatch` from `@/lib/api-client`. Import `useApiMutation` from `@/hooks/use-api-mutation`. Import `useQueryClient` from `@tanstack/react-query`.

Add this function:
```typescript
export function useUpdateHouseholdSettings() {
  const queryClient = useQueryClient();
  return useApiMutation({
    mutationFn: (data: { base_currency?: string }) =>
      apiPatch<{ id: string; name: string; base_currency: string }>(
        "/api/v1/households",
        data
      ),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["household"] });
      queryClient.invalidateQueries({ queryKey: ["dashboard"] });
      queryClient.invalidateQueries({ queryKey: ["net-worth"] });
      queryClient.invalidateQueries({ queryKey: ["accounts"] });
    },
  });
}
```
  </action>
  <verify>
    <automated>cd /home/gharib/projects/masareef/frontend && pnpm exec tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` contains `"react-plotly.js"` and `"plotly.js-dist-min"` in dependencies
    - `frontend/package.json` contains `"@types/react-plotly.js"` in devDependencies
    - `frontend/src/components/ui/skeleton.tsx` exists (shadcn installed)
    - `frontend/src/components/ui/toggle.tsx` exists (shadcn installed)
    - `frontend/src/lib/api-client.ts` contains `export async function apiPatch`
    - `frontend/src/lib/types/dashboard.ts` contains `export interface StatCardsData`, `export interface IncomeVsExpensesMonth`, `export interface SpendingByCategory`, `export interface NetWorthTrendPoint`
    - `frontend/src/hooks/use-dashboard.ts` contains `export function useIncomeVsExpenses`, `export function useSpendingByCategory`, `export function useNetWorthTrend`, `export function useStatCards`
    - All hooks use `queryKey: ["dashboard",` prefix
    - All hooks set `staleTime: 60_000`
    - `frontend/src/hooks/use-households.ts` contains `export function useUpdateHouseholdSettings`
    - `useUpdateHouseholdSettings` calls `apiPatch("/api/v1/households"`
    - `pnpm exec tsc --noEmit` exits with 0 errors
  </acceptance_criteria>
  <done>All frontend npm dependencies installed. Dashboard types mirror backend schemas. 4 TanStack Query hooks fetch dashboard endpoints. apiPatch exists. Household update mutation wired.</done>
</task>

<task type="auto">
  <name>Task 2: Supporting UI components (skeleton, toggles, selector, delta card, stat card enhancement) + i18n</name>
  <files>frontend/src/components/dashboard/chart-skeleton.tsx, frontend/src/components/dashboard/time-range-toggle.tsx, frontend/src/components/dashboard/compare-toggle.tsx, frontend/src/components/dashboard/base-currency-selector.tsx, frontend/src/components/dashboard/delta-summary-card.tsx, frontend/src/components/shared/stat-card.tsx, frontend/messages/en.json, frontend/messages/ar.json</files>
  <read_first>
    - frontend/src/components/shared/stat-card.tsx
    - frontend/src/components/ui/skeleton.tsx (installed in Task 1)
    - frontend/src/components/ui/toggle.tsx (installed in Task 1)
    - frontend/src/components/ui/card.tsx
    - frontend/src/components/ui/select.tsx
    - frontend/src/lib/money.ts
    - frontend/src/lib/utils.ts
    - frontend/messages/en.json
    - frontend/messages/ar.json
    - frontend/src/app/globals.css
    - .planning/phases/02-dashboard-charts/02-UI-SPEC.md
  </read_first>
  <action>
**Component 1: `frontend/src/components/dashboard/chart-skeleton.tsx`**
```typescript
"use client";

import { Card } from "@/components/ui/card";
import { Skeleton } from "@/components/ui/skeleton";
import { cn } from "@/lib/utils";

interface ChartSkeletonProps {
  variant?: "area" | "bar" | "donut";
  className?: string;
}

export function ChartSkeleton({ variant = "area", className }: ChartSkeletonProps) {
  return (
    <Card className={cn("p-6", className)}>
      <Skeleton className="h-5 w-40 mb-4" /> {/* chart title */}
      <div className="h-64 flex items-end justify-center gap-2">
        {variant === "bar" && (
          <>
            {Array.from({ length: 6 }).map((_, i) => (
              <div key={i} className="flex gap-1 items-end flex-1">
                <Skeleton className="flex-1" style={{ height: `${40 + Math.random() * 60}%` }} />
                <Skeleton className="flex-1" style={{ height: `${30 + Math.random() * 50}%` }} />
              </div>
            ))}
          </>
        )}
        {variant === "donut" && (
          <Skeleton className="h-48 w-48 rounded-full" />
        )}
        {variant === "area" && (
          <Skeleton className="h-full w-full rounded-md" />
        )}
      </div>
    </Card>
  );
}
```

**Component 2: `frontend/src/components/dashboard/time-range-toggle.tsx`**
Per UI-SPEC: segmented control, 1M/3M/6M/1Y/All. Active gets `bg-card shadow-sm rounded-md text-primary font-bold`. Inactive gets transparent `text-muted-foreground font-normal`. Buttons sized `py-2 px-4 text-xs`.
```typescript
"use client";

import { useTranslations } from "next-intl";
import { cn } from "@/lib/utils";

type TimeRange = "1M" | "3M" | "6M" | "1Y" | "All";

interface TimeRangeToggleProps {
  value: TimeRange;
  onChange: (range: TimeRange) => void;
}

const RANGES: TimeRange[] = ["1M", "3M", "6M", "1Y", "All"];
const MONTHS_MAP: Record<TimeRange, number | undefined> = {
  "1M": 1, "3M": 3, "6M": 6, "1Y": 12, "All": undefined,
};

export function timeRangeToMonths(range: TimeRange): number | undefined {
  return MONTHS_MAP[range];
}

// i18n keys: dashboard.timeRange.1M, .3M, .6M, .1Y, .All
const I18N_KEYS: Record<TimeRange, string> = {
  "1M": "1M", "3M": "3M", "6M": "6M", "1Y": "1Y", "All": "All",
};

export function TimeRangeToggle({ value, onChange }: TimeRangeToggleProps) {
  const t = useTranslations("dashboard.timeRange");

  return (
    <div className="inline-flex items-center gap-1 rounded-lg bg-muted p-1" role="radiogroup" aria-label="Time range">
      {RANGES.map((range) => (
        <button
          key={range}
          role="radio"
          aria-checked={value === range}
          onClick={() => onChange(range)}
          className={cn(
            "py-2 px-4 text-xs rounded-md transition-colors",
            value === range
              ? "bg-card shadow-sm text-primary font-bold"
              : "text-muted-foreground font-normal hover:text-foreground"
          )}
        >
          {t(I18N_KEYS[range])}
        </button>
      ))}
    </div>
  );
}
```

**Component 3: `frontend/src/components/dashboard/compare-toggle.tsx`**
Per UI-SPEC: toggle switch for month-over-month comparison. Active state uses `text-primary`.
```typescript
"use client";

import { useTranslations } from "next-intl";
import { cn } from "@/lib/utils";

interface CompareToggleProps {
  enabled: boolean;
  onChange: (enabled: boolean) => void;
}

export function CompareToggle({ enabled, onChange }: CompareToggleProps) {
  const t = useTranslations("dashboard");

  return (
    <button
      onClick={() => onChange(!enabled)}
      className={cn(
        "flex items-center gap-2 text-xs rounded-md px-3 py-1.5 transition-colors border",
        enabled
          ? "bg-primary/10 border-primary/20 text-primary font-bold"
          : "bg-transparent border-border text-muted-foreground font-normal hover:text-foreground"
      )}
      aria-pressed={enabled}
    >
      {t("compare")}
    </button>
  );
}
```

**Component 4: `frontend/src/components/dashboard/base-currency-selector.tsx`**
Per D-12: dropdown using shadcn Select. Reads household's current base_currency. Writes via PATCH.
```typescript
"use client";

import { useTranslations } from "next-intl";
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select";

// CURRENCIES from CLAUDE.md Section D
const CURRENCIES = [
  { code: "EGP", label: "EGP" },
  { code: "USD", label: "USD" },
  { code: "EUR", label: "EUR" },
  { code: "GBP", label: "GBP" },
  { code: "SAR", label: "SAR" },
  { code: "AED", label: "AED" },
  { code: "KWD", label: "KWD" },
];

interface BaseCurrencySelectorProps {
  value: string;
  onChange: (currency: string) => void;
}

export function BaseCurrencySelector({ value, onChange }: BaseCurrencySelectorProps) {
  const t = useTranslations("dashboard");

  return (
    <div className="flex items-center gap-2">
      <span className="text-xs text-muted-foreground">{t("baseCurrency")}</span>
      <Select value={value} onValueChange={onChange}>
        <SelectTrigger className="w-24 h-8 text-xs">
          <SelectValue />
        </SelectTrigger>
        <SelectContent>
          {CURRENCIES.map((c) => (
            <SelectItem key={c.code} value={c.code}>
              {c.label}
            </SelectItem>
          ))}
        </SelectContent>
      </Select>
    </div>
  );
}
```

**Component 5: `frontend/src/components/dashboard/delta-summary-card.tsx`**
Per D-07: shows current vs previous month delta when compare is active.
```typescript
"use client";

import { useTranslations, useLocale } from "next-intl";
import { TrendingUp, TrendingDown, Minus } from "lucide-react";
import { Card } from "@/components/ui/card";
import { formatAmount, formatAmountAr } from "@/lib/money";
import { cn } from "@/lib/utils";

interface DeltaSummaryCardProps {
  currentMinor: number;
  previousMinor: number;
  currency: string;
}

export function DeltaSummaryCard({ currentMinor, previousMinor, currency }: DeltaSummaryCardProps) {
  const t = useTranslations("dashboard");
  const locale = useLocale();
  const fmt = (v: number) => locale === "ar" ? formatAmountAr(v, currency) : formatAmount(v, currency);

  const delta = currentMinor - previousMinor;
  const pct = previousMinor !== 0 ? Math.round(Math.abs(delta) / Math.abs(previousMinor) * 100) : null;
  const direction = delta > 0 ? "up" : delta < 0 ? "down" : "flat";

  const Icon = direction === "up" ? TrendingUp : direction === "down" ? TrendingDown : Minus;

  return (
    <Card className="p-3 flex items-center justify-between gap-4 text-xs">
      <div className="flex items-center gap-4">
        <span className="text-muted-foreground">{t("currentMonth")}: <span className="font-bold text-foreground">{fmt(currentMinor)}</span></span>
        <span className="text-muted-foreground">{t("previousMonth")}: <span className="font-bold text-foreground">{fmt(previousMinor)}</span></span>
      </div>
      <div className={cn(
        "flex items-center gap-1 font-bold",
        direction === "up" && "text-destructive",
        direction === "down" && "text-primary",
        direction === "flat" && "text-muted-foreground",
      )}>
        <Icon className="h-3.5 w-3.5" />
        <span>{fmt(Math.abs(delta))}</span>
        {pct !== null && <span>({pct}%)</span>}
        <span className="font-normal text-muted-foreground ms-1">{t("vsLastMonth")}</span>
      </div>
    </Card>
  );
}
```

**Enhance `frontend/src/components/shared/stat-card.tsx`**
Update the `trend` prop type to include optional `percentChange`:
```typescript
trend?: {
  direction: "up" | "down" | "flat";
  text: string;
  percentChange?: number | null;  // NEW: percentage change, null when prev was 0
};
```
In the trend display JSX (the existing `<p>` that shows trend), append the percentage after the text:
```tsx
{trend.text}
{trend.percentChange != null && (
  <span className="ms-1">({trend.percentChange > 0 ? "+" : ""}{Math.round(trend.percentChange)}%)</span>
)}
```

**Update i18n: `frontend/messages/en.json`**
Add/update these keys under the `"dashboard"` namespace:
```json
{
  "dashboard": {
    // ... keep ALL existing keys ...
    "baseCurrency": "Base Currency",
    "compare": "Compare",
    "vsLastMonth": "vs. last month",
    "currentMonth": "This month",
    "previousMonth": "Last month",
    "netWorthTrend": "Net Worth Trend",
    "cashFlow": "Cash Flow",
    "monthlySpending": "Monthly Spending",
    "spentThisMonth": "Spent This Month",
    "dueNext30Days": "Due Next 30 Days",
    "noDataForPeriod": "No data for this period",
    "addTransactionsToSee": "Add transactions to see your financial trends here.",
    "startTrackingNetWorth": "Start tracking your net worth",
    "addAccountsAndRecord": "Add accounts and record transactions to see your net worth trend over time.",
    "noSpendingThisMonth": "No spending this month",
    "categorizedWillAppear": "Categorized transactions will appear as a breakdown here.",
    "total": "Total",
    "income": "Income",
    "expenses": "Expenses",
    "tapToRetry": "Tap to retry",
    "couldntLoadChart": "This chart couldn't load. Tap to retry.",
    "couldntLoadDashboard": "Couldn't load your dashboard. Check your connection and try again.",
    "fxWarning": "Some amounts may be inaccurate — exchange rates are temporarily unavailable.",
    "timeRange": {
      "1M": "1M",
      "3M": "3M",
      "6M": "6M",
      "1Y": "1Y",
      "All": "All"
    }
  }
}
```

**Update i18n: `frontend/messages/ar.json`**
Add/update matching keys under `"dashboard"`:
```json
{
  "dashboard": {
    // ... keep ALL existing keys ...
    "baseCurrency": "العملة الأساسية",
    "compare": "مقارنة",
    "vsLastMonth": "مقارنة بالشهر الماضي",
    "currentMonth": "هذا الشهر",
    "previousMonth": "الشهر الماضي",
    "netWorthTrend": "اتجاه صافي الثروة",
    "cashFlow": "التدفق النقدي",
    "monthlySpending": "الإنفاق الشهري",
    "spentThisMonth": "إنفاق هذا الشهر",
    "dueNext30Days": "مستحق خلال 30 يوم",
    "noDataForPeriod": "لا توجد بيانات لهذه الفترة",
    "addTransactionsToSee": "أضف معاملات لرؤية اتجاهاتك المالية هنا.",
    "startTrackingNetWorth": "ابدأ بتتبع صافي ثروتك",
    "addAccountsAndRecord": "أضف حسابات وسجّل معاملات لرؤية اتجاه صافي ثروتك عبر الزمن.",
    "noSpendingThisMonth": "لا يوجد إنفاق هذا الشهر",
    "categorizedWillAppear": "ستظهر المعاملات المصنّفة كتوزيع هنا.",
    "total": "الإجمالي",
    "income": "الدخل",
    "expenses": "المصروفات",
    "tapToRetry": "انقر لإعادة المحاولة",
    "couldntLoadChart": "تعذّر تحميل هذا الرسم البياني. انقر لإعادة المحاولة.",
    "couldntLoadDashboard": "تعذّر تحميل لوحة المعلومات. تحقق من اتصالك وحاول مرة أخرى.",
    "fxWarning": "قد تكون بعض المبالغ غير دقيقة — أسعار الصرف غير متوفرة مؤقتًا.",
    "timeRange": {
      "1M": "1ش",
      "3M": "3ش",
      "6M": "6ش",
      "1Y": "1س",
      "All": "الكل"
    }
  }
}
```

CRITICAL: When editing en.json and ar.json, preserve ALL existing keys. Only add the new keys listed above. Do NOT remove or modify existing keys like `"title"`, `"subtitle"`, `"netWorth"`, `"monthlyIncome"`, etc.

All new components MUST use logical CSS properties only — no `pl-`, `pr-`, `ml-`, `mr-`, `left-`, `right-`, `text-left`, `text-right`. Use `ps-`, `pe-`, `ms-`, `me-`, `start-`, `end-`, `text-start`, `text-end` instead.
  </action>
  <verify>
    <automated>cd /home/gharib/projects/masareef/frontend && pnpm exec tsc --noEmit 2>&1 | head -20 && pnpm lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/dashboard/chart-skeleton.tsx` contains `export function ChartSkeleton` with `animate-pulse` via Skeleton component
    - `frontend/src/components/dashboard/time-range-toggle.tsx` contains `export function TimeRangeToggle` with `role="radiogroup"` and 5 buttons (1M, 3M, 6M, 1Y, All)
    - `frontend/src/components/dashboard/time-range-toggle.tsx` exports `timeRangeToMonths` function
    - `frontend/src/components/dashboard/compare-toggle.tsx` contains `export function CompareToggle` with `aria-pressed`
    - `frontend/src/components/dashboard/base-currency-selector.tsx` contains `export function BaseCurrencySelector` with shadcn Select
    - `frontend/src/components/dashboard/delta-summary-card.tsx` contains `export function DeltaSummaryCard` with current/previous/delta display
    - `frontend/src/components/shared/stat-card.tsx` trend prop type includes `percentChange?: number | null`
    - `frontend/messages/en.json` contains `"baseCurrency"`, `"compare"`, `"vsLastMonth"`, `"timeRange"` keys under `"dashboard"`
    - `frontend/messages/ar.json` contains `"baseCurrency": "العملة الأساسية"`, `"timeRange": { "1M": "1ش" }` under `"dashboard"`
    - No physical directional CSS classes (`pl-`, `pr-`, `ml-`, `mr-`, `left-`, `right-`) in any new component files
    - `pnpm exec tsc --noEmit` exits with 0 errors
  </acceptance_criteria>
  <done>All 5 supporting UI components created. StatCard enhanced with percentage trend. All i18n keys in en.json and ar.json. TypeScript compiles cleanly. No physical directional CSS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> API | All data fetched via authenticated API calls (Supabase JWT) |
| user input -> state | Currency selector and time range are constrained to predefined values |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | ASVS | Disposition | Mitigation | Task |
|-----------|----------|-----------|----------|------|-------------|------------|------|
| T-02-05 | Tampering | BaseCurrencySelector | low | V5.1.1 | mitigate | Currency codes constrained to hardcoded CURRENCIES array — no free-text input. Backend also validates max_length=3. | Task 2 |
| T-02-06 | Information Disclosure | api-client | low | V3.1.1 | accept | All API calls authenticated via Supabase session token. No PII in new components beyond existing pattern. | Task 1 |
</threat_model>

<verification>
```bash
cd /home/gharib/projects/masareef/frontend
pnpm exec tsc --noEmit
pnpm lint
# Verify no physical directional CSS in new files
grep -rn 'pl-\|pr-\|ml-\|mr-\|\bleft-\|\bright-\|text-left\|text-right' src/components/dashboard/chart-skeleton.tsx src/components/dashboard/time-range-toggle.tsx src/components/dashboard/compare-toggle.tsx src/components/dashboard/base-currency-selector.tsx src/components/dashboard/delta-summary-card.tsx || echo "No physical directional CSS found (GOOD)"
```
</verification>

<success_criteria>
- react-plotly.js and plotly.js-dist-min installed in package.json
- shadcn skeleton and toggle components installed
- apiPatch exists in api-client.ts
- Dashboard types match backend schemas
- 4 TanStack Query hooks with ["dashboard", ...] query keys
- Household update mutation invalidates dashboard queries
- 5 new dashboard components created with correct props
- StatCard trend shows percentage
- All i18n keys in both en.json and ar.json
- TypeScript compiles with zero errors
- No physical directional CSS in any new files
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-charts/02-02-SUMMARY.md`
</output>
