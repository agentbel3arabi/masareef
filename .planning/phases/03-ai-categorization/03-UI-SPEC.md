---
phase: 3
slug: ai-categorization
status: draft
shadcn_initialized: true
preset: base-nova
created: 2026-04-08
revised: 2026-04-08
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | base-nova (base-ui/react primitives) |
| Component library | @base-ui/react |
| Icon library | lucide-react |
| Font | Inter (Latin), Noto Sans Arabic (Arabic) |

Source: `frontend/components.json` (confirmed `"style": "base-nova"`, `"iconLibrary": "lucide"`, `"rtl": true`)

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Badge inner padding (`px-1 py-0.5`), icon-to-text gaps |
| sm | 8px | Compact element spacing, filter bar gaps, table cell padding (`py-2`) |
| md | 16px | Default element spacing, table cell horizontal padding (`px-4 py-3`), form field gaps |
| lg | 24px | Section padding, card body padding |
| xl | 32px | Layout gaps between major sections |
| 2xl | 48px | Rules management page section breaks |
| 3xl | 64px | Page-level spacing (not used in this phase) |

Exceptions:
- AI badge touch area: minimum 24px height (`inline-flex items-center rounded-full px-2 py-0.5`)
- Bulk approve toolbar height: 48px (`h-12`) to meet touch target requirements on mobile

Source: existing `transaction-row.tsx` (`px-4 py-3`), `transaction-filters.tsx` (`px-3 py-2`)

---

## Typography

Exactly 2 weights. Labels and body both use 400 — weight contrast is achieved via size and color, not additional weight steps.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (`text-sm`) | 400 (`font-normal`) | 1.5 |
| Label | 12px (`text-xs`) | 400 (`font-normal`) | 1.4 |
| Heading | 20px (`text-xl`) | 700 (`font-bold`) | 1.2 |
| Badge/Chip | 10px (`text-[10px]`) | 700 (`font-bold`) | 1.0 |

Notes:
- 4 sizes total — body, label, heading, badge. No display size needed this phase.
- `font-medium` (weight 500) is not used anywhere in this phase. All body and label text uses `font-normal`.
- Badge tier maps to existing badge pattern from `transaction-row.tsx` (`text-[10px]` + `font-bold`).
- Arabic text uses Noto Sans Arabic at the same size scale; `font-sans` CSS variable already includes Noto Sans Arabic.

Source: `globals.css` (`--font-sans` includes both fonts), `transaction-row.tsx` (existing size usage)

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#FFFFFF` / `hsl(0 0% 100%)` light; `#0F172A` dark | Page background (`--background`) |
| Secondary (30%) | `#F8FAFC` / `hsl(210 40% 98%)` light | Cards, sidebar, filter bar, table header (`--surface`, `--muted`) |
| Accent (10%) | `#16A34A` / `hsl(142.1 76.2% 36.3%)` | Reserved list below |
| Destructive | `#EF4444` / `hsl(0 84.2% 60.2%)` | Delete rule button, delete confirmation dialog |
| Warning | `#F59E0B` / `hsl(37.7 92.1% 50.2%)` | AI badge: yellow tier (75–95% confidence), token budget 80% threshold toast |

Accent (`#16A34A`) reserved for:
- Primary CTA buttons: "Approve All", "Save Rule", "Categorize Now"
- Active nav item in settings sidebar (left border indicator)
- Positive state: AI badge green tier (>95% confidence)
- Bulk-approve button fill
- Focus ring (`--ring`)

Do NOT use accent for: hover states on table rows, filter selects, secondary actions, or badges on pending/low-confidence items.

Source: `globals.css` (`--primary: 142.1 76.2% 36.3%`, `--destructive: 0 84.2% 60.2%`, `--warning: 37.7 92.1% 50.2%`), `docs/guides/09-design-tokens.md`

---

## AI Badge Color Contract

Three tiers, color-coded per D-08 (locked decision):

| Tier | Condition | Background | Text | Tailwind |
|------|-----------|------------|------|---------|
| Green — auto-applied | `confidence > 0.95` | `green-100` | `green-700` | `bg-green-100 text-green-700` |
| Yellow — suggested | `0.75 <= confidence <= 0.95` | `yellow-100` | `yellow-700` | `bg-yellow-100 text-yellow-700` |
| Red — low confidence | `confidence < 0.75` | `red-100` | `red-700` | `bg-red-100 text-red-700` |

Badge anatomy: `inline-flex items-center rounded-full px-2 py-0.5 text-[10px] font-bold`

Badge label: `AI {confidence%}` — e.g., "AI 98%", "AI 82%", "AI 61%"

Arabic label: `ذكاء اصطناعي {confidence%}` — uses same badge structure, RTL-aware (no physical directional classes).

Placement: inline, immediately after the category chip in the category column of the transaction table. Separator: 4px gap (`gap-1`).

Source: CONTEXT.md D-08, RESEARCH.md Pattern 6

---

## Visual Focal Point

### Transactions Page (Extended)

**Primary focal point:** The "Needs Review" filter toggle is the entry point to the AI workflow. It is the first element users activate to enter AI review mode. Its active state (filled `bg-primary text-primary-foreground`) should be visually distinct from all other filter buttons, making it the clear call-to-action anchor on the filter bar.

**Secondary focal point:** The `AiBadge` column is the sustained visual anchor once the filter is active — the color-coded badges (green/yellow/red) across transaction rows guide the user's eye to confidence tiers and required actions.

**Hierarchy rule:** When `needs_review` filter is inactive, the `AiBadge` instances that appear are supplementary indicators, not focal points — they must not compete visually with the category chip they annotate. When active, the badge becomes primary signal.

### Settings — Categorization Rules Page

**Primary focal point:** The rules table is the dominant element. The "Backfill Past Transactions" button is a secondary action and must not visually compete with the table content.

---

## Component Inventory

New components to create this phase:

| Component | Path | Based On |
|-----------|------|----------|
| `AiBadge` | `frontend/src/components/transactions/ai-badge.tsx` | Existing badge pattern from `transaction-row.tsx` |
| `CategorizationRulesTable` | `frontend/src/components/settings/categorization-rules.tsx` | Existing table pattern from `transaction-table.tsx` |
| `BulkApproveToolbar` | Extend existing `frontend/src/components/transactions/bulk-toolbar.tsx` | Add "Approve All AI Suggestions" button to existing toolbar |

Existing components to extend:

| Component | Extension |
|-----------|-----------|
| `TransactionFilterBar` | Add "Needs review" toggle filter (`needs_review: boolean`) |
| `TransactionRow` | Render `AiBadge` when `ai_categorized === true` |
| `TransactionTable` | No change — row extension is sufficient |

Reuse existing shadcn components: `Button`, `Badge`, `Dialog`, `AlertDialog`, `Card`, `Input`, `Label`, `Select`, `Skeleton`, `Separator`, `Tooltip`.

No new shadcn components need to be installed. No third-party registries.

---

## Page and View Inventory

| View | Route | Type |
|------|-------|------|
| Transactions page (extended) | `/transactions` | Existing page, extended with "Needs review" filter + AI badges |
| Settings — Categorization Rules | `/settings/categorization` | New page |

Design references:
- `docs/stitch-designs/html/07b-transactions-global.html` — transactions page layout (filter bar, table structure)
- `docs/stitch-designs/html/19c-settings-categories.html` — settings page layout, sidebar nav, two-column structure, category row anatomy

---

## Interaction Contract

### "Needs Review" Filter

- Rendered as a toggle button in the filter bar, after the existing type filter.
- Label: "Needs Review" (EN) / "تحتاج مراجعة" (AR)
- Active state: filled background using `bg-primary text-primary-foreground`; inactive: `variant="outline"`
- Activating the filter: sets `needs_review=true` query param, clears page to 1
- Clearing: deactivate toggle OR click existing reset filters button

### AI Badge Interaction

- Badges are non-interactive (display-only) for green tier (>95%)
- Badges for yellow/red tiers carry a `Tooltip` on hover: "AI suggested — click category to correct"
- Clicking the badge itself does nothing; user corrects by opening the existing edit flow (pencil icon)
- After category change (correction): badge disappears; rule is created silently in the background

### Bulk Approve

- "Approve All AI Suggestions" button appears only when `needs_review` filter is active AND result count > 0
- Placement: right side of filter bar, separated by `Separator` (vertical)
- Button variant: `default` (filled green, primary CTA)
- Click triggers: `POST /api/v1/transactions/categorize-batch` with approve=true for all visible IDs
- Loading state: button disabled + spinner icon replacing lucide `CheckCheck` icon
- Success: toast "N transactions approved" + TanStack Query invalidation of `["transactions"]`
- Error: toast "Failed to approve — try again"

### Categorization Rules Page

- Layout: settings two-column (secondary sidebar + main canvas), matching `19c-settings-categories.html` pattern
- Main canvas: table with columns — Pattern, Match Type, Category, Hit Count, Confidence, Actions
- Row actions: Edit (pencil icon, inline, opens popover), Delete (trash icon, shows `AlertDialog` confirmation)
- **Row action icon accessibility:** Both edit and delete actions are icon-only buttons. Each must carry an `aria-label` attribute — no visible label is shown. Required `aria-label` values: edit button → `"Edit rule"` / `"تعديل القاعدة"` (AR); delete button → `"Delete rule"` / `"حذف القاعدة"` (AR). Implement as `<button aria-label="Edit rule">` with the lucide `Pencil` icon as child.
- "Backfill Past Transactions" button: secondary placement below table header, `variant="outline"`, triggers on-demand batch
- Empty state: no table shown; centered illustration placeholder + copy (see Copywriting section)
- Loading state: `Skeleton` rows (5 rows), same column structure

### On-Demand Categorization Button (Transactions Page)

- Label: "Categorize Uncategorized"
- Placement: right side of filter bar, visible only when no `needs_review` filter active
- Variant: `variant="outline"`
- Click: `POST /api/v1/transactions/categorize-batch` for uncategorized in current filter scope
- Loading: disabled + spinner
- Success: toast "Categorization started — results will appear shortly" (async, background task)

---

## Loading States

| Surface | Loading Pattern |
|---------|----------------|
| Transactions table (initial) | Existing skeleton — no change |
| AI badge pending (after bulk approve) | Badge replaced with `Skeleton` (`h-4 w-12 rounded-full`) until invalidation resolves |
| Rules table | 5 `Skeleton` rows, same column widths as loaded state |
| Bulk approve in flight | Button disabled + `Loader2` spinning icon, label "Approving..." |
| Categorize batch in flight | Button disabled + `Loader2`, label "Categorizing..." |

---

## Copywriting Contract

| Element | EN Copy | AR Copy |
|---------|---------|---------|
| Primary CTA — approve | "Approve All" | "موافقة على الكل" |
| Primary CTA — categorize | "Categorize Now" | "تصنيف الآن" |
| Filter toggle — needs review | "Needs Review" | "تحتاج مراجعة" |
| AI badge tooltip | "AI suggested — click category to correct" | "اقتراح ذكاء اصطناعي — انقر على الفئة للتصحيح" |
| Bulk approve success toast | "{N} transactions approved" | "تمت الموافقة على {N} معاملة" |
| Categorize batch success toast | "Categorization started — results appear shortly" | "بدأ التصنيف — ستظهر النتائج قريبًا" |
| Empty state heading — needs review | "All caught up" | "أنت على ما يرام" |
| Empty state body — needs review | "No transactions need review. AI has categorized everything it can." | "لا توجد معاملات تحتاج إلى مراجعة. صنّف الذكاء الاصطناعي كل ما يستطيع." |
| Empty state heading — rules page | "No rules yet" | "لا توجد قواعد بعد" |
| Empty state body — rules page | "Rules are created automatically when you correct a category. Your first correction will appear here." | "تُنشأ القواعد تلقائيًا عند تصحيح فئة. سيظهر تصحيحك الأول هنا." |
| Error — batch categorize failed | "Categorization failed. Check your AI settings and try again." | "فشل التصنيف. تحقق من إعدادات الذكاء الاصطناعي وحاول مجددًا." |
| Error — approve failed | "Approval failed. Please try again." | "فشلت الموافقة. يرجى المحاولة مجددًا." |
| Error — budget exhausted | "AI budget exhausted for this month. Transactions will be marked as Uncategorized." | "استُنفد حد الذكاء الاصطناعي لهذا الشهر. ستُصنَّف المعاملات ضمن 'غير مصنف'." |
| Delete rule — confirmation title | "Delete this rule?" | "حذف هذه القاعدة؟" |
| Delete rule — confirmation body | "The rule for '{pattern}' will be deleted. Future transactions won't be auto-categorized by this rule." | "سيتم حذف القاعدة الخاصة بـ '{pattern}'. لن يتم تصنيف المعاملات المستقبلية تلقائيًا بهذه القاعدة." |
| Delete rule — confirm button | "Delete Rule" | "حذف القاعدة" |
| Backfill button | "Categorize Past Transactions" | "تصنيف المعاملات السابقة" |
| Edit rule aria-label | "Edit rule" | "تعديل القاعدة" |
| Delete rule aria-label | "Delete rule" | "حذف القاعدة" |

---

## Destructive Actions

| Action | Trigger | Confirmation Pattern |
|--------|---------|---------------------|
| Delete categorization rule | Trash icon button in rules table row (icon-only, `aria-label="Delete rule"`) | `AlertDialog` — title + body copy above; confirm button `variant="destructive"` ("Delete Rule"); cancel ("Cancel" / "إلغاء") |

No other destructive actions in this phase. Rule edits are non-destructive (can be re-edited).

---

## RTL Compliance

All new components must use logical CSS properties only:

| Forbidden | Required |
|-----------|---------|
| `pl-`, `pr-` | `ps-`, `pe-` |
| `ml-`, `mr-` | `ms-`, `me-` |
| `left-`, `right-` | `start-`, `end-` |
| `text-left`, `text-right` | `text-start`, `text-end` |
| `border-l-`, `border-r-` | `border-s-`, `border-e-` |

After installing any new shadcn component via `pnpm dlx shadcn@latest add`, audit immediately for physical directional classes and convert.

Source: `CLAUDE.md` §E Rule 4, RESEARCH.md Project Constraints

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | badge, dialog, alert-dialog, skeleton, tooltip, separator (all already installed) | not required |

No third-party registries. Confirmed from `frontend/components.json`: `"registries": {}`.

No new shadcn components need to be added this phase — all required components are already in `frontend/src/components/ui/`.

Safety gate status: view passed — no third-party registries — 2026-04-08

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
