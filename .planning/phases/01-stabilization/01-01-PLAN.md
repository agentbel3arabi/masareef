---
phase: 01-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BACKLOG.md
  - CLAUDE.md
  - docs/01-architecture.md
  - docs/02-data-models.md
  - docs/03-features/accounts.md
  - docs/03-features/transactions.md
  - docs/03-features/transfers.md
  - docs/03-features/import.md
  - docs/03-features/categories.md
  - docs/03-features/debts.md
  - docs/05-roadmap.md
  - docs/guides/08-testing.md
autonomous: true
requirements:
  - STAB-01
  - STAB-02

must_haves:
  truths:
    - "BACKLOG.md has BL-027, BL-028, BL-029 tagged to Phase 1 (not Phase 4)"
    - "BACKLOG.md has BL-032 tagged to Phase 1 (not Phase 10)"
    - "docs/05-roadmap.md shows Phase 3.8 as complete"
    - "No conflicting information exists between CLAUDE.md, architecture doc, and feature specs"
  artifacts:
    - path: "BACKLOG.md"
      provides: "Re-tagged backlog items for Phase 1"
      contains: "Phase 1"
    - path: "docs/05-roadmap.md"
      provides: "Accurate phase statuses"
      contains: "Phase 3.8"
  key_links:
    - from: "docs/02-data-models.md"
      to: "backend/app/models/"
      via: "Schema definitions must match ORM models"
      pattern: "table schemas"
---

<objective>
Audit and align all project documentation with the actual codebase state, re-tag BACKLOG.md items to match the new ROADMAP.md phase assignments, and mark completed phases as done.

Purpose: Establishes ground truth for all subsequent Phase 1 work. Every plan that follows depends on accurate documentation.
Output: Updated CLAUDE.md, BACKLOG.md, roadmap, architecture doc, data models doc, feature specs, and testing guide -- all verified against the actual codebase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilization/01-CONTEXT.md
@.planning/phases/01-stabilization/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-tag BACKLOG.md and update roadmap status</name>
  <files>BACKLOG.md, docs/05-roadmap.md</files>
  <read_first>
    - BACKLOG.md
    - docs/05-roadmap.md
    - .planning/ROADMAP.md
    - .planning/phases/01-stabilization/01-CONTEXT.md
  </read_first>
  <action>
Per D-02, update BACKLOG.md:
1. Change BL-027 target from "Phase 4" to "Phase 1"
2. Change BL-028 target from "Phase 4" to "Phase 1"
3. Change BL-029 target from "Phase 4" to "Phase 1"
4. Change BL-032 target from "Phase 10 — Multi-User" to "Phase 1"
5. Update the summary table header counts to reflect the re-tagging (Phase 1 gains 4 items, Phase 4 loses 3, Phase 10 loses 1)
6. Per D-03, do NOT re-tag BL-038, BL-039, BL-040, BL-043, BL-044, BL-048 -- they stay unscheduled

Per STAB-02, update docs/05-roadmap.md:
1. Find "Phase 3.8" entry and mark it as complete (checkbox checked: `[x]`)
2. Verify all earlier phases (1-3.75) are also marked complete if they were completed
3. Update any stale status text that says "in progress" for phases that are actually done
  </action>
  <verify>
    <automated>grep -n "Phase 1" BACKLOG.md | grep -E "BL-02[789]|BL-032" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - BACKLOG.md line for BL-027 contains "Phase 1" (not "Phase 4")
    - BACKLOG.md line for BL-028 contains "Phase 1" (not "Phase 4")
    - BACKLOG.md line for BL-029 contains "Phase 1" (not "Phase 4")
    - BACKLOG.md line for BL-032 contains "Phase 1" (not "Phase 10")
    - BACKLOG.md summary table counts are updated
    - BL-038 still contains "Unscheduled"
    - BL-039 still contains "Unscheduled"
    - docs/05-roadmap.md contains "[x]" next to Phase 3.8
  </acceptance_criteria>
  <done>BL-027/028/029/032 re-tagged to Phase 1, roadmap status reflects reality, unscheduled items unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Systematic doc audit — verify docs match codebase</name>
  <files>CLAUDE.md, docs/01-architecture.md, docs/02-data-models.md, docs/03-features/accounts.md, docs/03-features/transactions.md, docs/03-features/transfers.md, docs/03-features/import.md, docs/03-features/categories.md, docs/03-features/debts.md, docs/guides/08-testing.md</files>
  <read_first>
    - CLAUDE.md
    - docs/01-architecture.md
    - docs/02-data-models.md
    - docs/03-features/accounts.md
    - docs/03-features/transactions.md
    - docs/03-features/transfers.md
    - docs/03-features/import.md
    - docs/03-features/categories.md
    - docs/03-features/debts.md
    - docs/guides/08-testing.md
    - backend/app/models/account.py
    - backend/app/models/transaction.py
    - backend/app/models/debt.py
    - backend/app/models/category.py
    - backend/app/models/financial_institution.py
    - backend/app/routers/accounts.py
    - backend/app/routers/transactions.py
    - backend/app/routers/debts.py
    - backend/app/routers/import_.py
    - backend/app/routers/transfers.py
    - backend/app/routers/categories.py
    - backend/app/schemas/account.py
    - backend/app/schemas/transaction.py
  </read_first>
  <action>
Per D-14, perform a systematic audit of all doc files against the actual codebase:

1. **docs/02-data-models.md vs actual ORM models**: For every table listed in 02-data-models.md, read the corresponding `backend/app/models/*.py` file and verify:
   - Column names match exactly
   - Column types match (BIGINT for amounts, UUID for IDs, etc.)
   - Enum values match between docs and `backend/app/models/enums.py`
   - Foreign key relationships match
   - Any columns in ORM models that are missing from docs must be added
   - Any columns in docs that no longer exist in ORM models must be removed

2. **docs/03-features/*.md vs actual routers**: For each feature spec, verify:
   - API endpoint paths match actual router prefixes and route definitions
   - Request body fields match actual Pydantic `*Create`/`*Update` schemas
   - Response fields match actual Pydantic `*Response` schemas
   - HTTP status codes match actual router return codes
   - Any endpoint documented but not implemented: add a note "[NOT YET IMPLEMENTED — Phase N]"
   - Any endpoint implemented but not documented: add documentation

3. **docs/01-architecture.md**: Verify:
   - Listed routers match actual files in `backend/app/routers/`
   - Listed services match actual files in `backend/app/services/`
   - Auth flow description matches actual `backend/app/dependencies.py` implementation
   - AI provider list matches actual files in `backend/app/ai/`

4. **CLAUDE.md**: Verify:
   - Technology versions match `backend/pyproject.toml` and `frontend/package.json`
   - Directory map matches actual directory structure
   - Dependency injection pattern matches actual `dependencies.py`
   - Task router table is accurate for current phase structure

5. **docs/guides/08-testing.md**: Verify:
   - Test directory structure description matches actual `backend/tests/` layout
   - The doc says `tests/services/` and `tests/models/` are empty but research found they have files -- update the doc to reflect actual contents
   - Fixture patterns match actual `conftest.py`
   - Coverage numbers reflect current state

Fix every discrepancy found. Do not skip or defer fixes.
  </action>
  <verify>
    <automated>cd /home/gharib/projects/masareef && grep -c "NOT YET IMPLEMENTED" docs/03-features/*.md; python3 -c "
import json
# Check CLAUDE.md tech stack versions align with package.json
pkg = json.loads(open('frontend/package.json').read())
deps = {**pkg.get('dependencies', {}), **pkg.get('devDependencies', {})}
claude = open('CLAUDE.md').read()
checks = []
if 'next' in deps: checks.append('next-in-pkg')
if 'next' in claude.lower(): checks.append('next-in-claude')
print('alignment-checks:', ','.join(checks))
"; ls backend/app/routers/*.py | wc -l; grep -c 'routers/' docs/01-architecture.md; echo "audit-complete"</automated>
  </verify>
  <acceptance_criteria>
    - docs/02-data-models.md column definitions match backend/app/models/*.py for all tables
    - docs/03-features/accounts.md endpoint paths match backend/app/routers/accounts.py route decorators
    - docs/03-features/transactions.md endpoint paths match backend/app/routers/transactions.py route decorators
    - docs/guides/08-testing.md no longer says tests/services/ and tests/models/ are empty
    - CLAUDE.md version numbers match pyproject.toml and package.json
    - docs/01-architecture.md lists all files in backend/app/routers/
    - Number of routers listed in architecture doc matches actual router file count
  </acceptance_criteria>
  <done>All documentation files verified against codebase. Every discrepancy found is fixed. Feature specs reflect actual API contracts. Data model doc matches ORM models. Testing guide reflects actual test infrastructure state. CLAUDE.md tech stack aligns with pyproject.toml and package.json.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected -- this plan modifies documentation only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | N/A | Documentation files | accept | Documentation-only changes, no runtime impact |
</threat_model>

<verification>
1. `grep -n "Phase 1" BACKLOG.md` shows BL-027, BL-028, BL-029, BL-032
2. `grep -n "Phase 4" BACKLOG.md` does NOT show BL-027, BL-028, BL-029
3. `grep "Unscheduled" BACKLOG.md` still shows BL-038, BL-039, BL-040, BL-043, BL-044, BL-048
4. docs/05-roadmap.md Phase 3.8 is marked complete
5. docs/guides/08-testing.md describes actual test file contents
6. Router count in docs/01-architecture.md matches `ls backend/app/routers/*.py | wc -l`
</verification>

<success_criteria>
All project documentation accurately reflects the current codebase state. BACKLOG.md items are tagged to correct phases per the new ROADMAP.md. No conflicting information exists between CLAUDE.md, architecture doc, data model doc, and feature specs.
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilization/01-01-SUMMARY.md`
</output>
