---
phase: 01-stabilization
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/app/services/transfer.py
  - BACKLOG.md
autonomous: true
requirements:
  - STAB-03

must_haves:
  truths:
    - "BL-029 stale TODO comment is removed from transfer.py"
    - "BL-029 is marked as Done in BACKLOG.md"
    - "The existing 4-way JOIN in list_transfers still works correctly"
  artifacts:
    - path: "backend/app/services/transfer.py"
      provides: "Clean transfer service without stale TODO"
    - path: "BACKLOG.md"
      provides: "BL-029 marked Done"
      contains: "Done"
  key_links:
    - from: "backend/app/services/transfer.py"
      to: "backend/app/routers/transfers.py"
      via: "list_transfers service call"
      pattern: "list_transfers"
---

<objective>
Verify BL-029 (N+1 in list_transfers) is already fixed, remove the stale TODO comment, and close the backlog item. This is a verification-and-cleanup step confirming the transfer JOIN is correct before heavier N+1 work in Plan 03.

Purpose: Closes one of the four Phase 1 bugs (BL-029) and validates the transfer service works correctly.
Output: Clean transfer.py without stale TODO, BL-029 marked Done in BACKLOG.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilization/01-CONTEXT.md
@.planning/phases/01-stabilization/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify BL-029 fix and write transfer list test</name>
  <files>backend/tests/routers/test_transfers.py, backend/app/services/transfer.py</files>
  <read_first>
    - backend/app/services/transfer.py
    - backend/app/routers/transfers.py
    - backend/tests/routers/test_transfers.py
    - backend/tests/conftest.py
  </read_first>
  <behavior>
    - Test 1: list_transfers returns both debit and credit account names in each transfer row (verifies JOIN loads both legs)
    - Test 2: list_transfers with multiple transfers returns correct count and all transfers have non-null from_account and to_account fields
  </behavior>
  <action>
Research confirmed that `list_transfers()` in `backend/app/services/transfer.py` (lines 156-241) already uses a 4-way JOIN to fetch debit leg + credit leg + from_account + to_account. The BL-029 N+1 issue was fixed in a prior phase.

1. Read `backend/app/services/transfer.py` lines 156-241 to confirm the JOIN query structure
2. Read `backend/tests/routers/test_transfers.py` to see existing test coverage
3. Add test(s) that specifically verify the JOIN works correctly:
   - Create two accounts, create a transfer between them
   - Call GET /api/v1/transfers
   - Assert the response includes both `from_account` and `to_account` populated with correct names
   - This test serves as a regression guard for the N+1 fix

4. Find and remove the stale TODO comment in `transfer.py`. Research says lines ~192-212 area. Look for any comment mentioning "N+1", "TODO", or "batch" related to the credit leg fetch.

5. Run existing transfer tests to confirm nothing is broken.

6. Update BACKLOG.md: change BL-029 status from "Open" to "Done" and add a note: "Already fixed — 4-way JOIN confirmed working. Stale TODO removed."
  </action>
  <verify>
    <automated>cd /home/gharib/projects/masareef/backend && uv run pytest tests/routers/test_transfers.py -x -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/services/transfer.py does NOT contain "TODO" related to N+1 or credit leg batch
    - backend/tests/routers/test_transfers.py contains a test that verifies from_account and to_account are populated in list response
    - All transfer tests pass (exit code 0)
    - BACKLOG.md BL-029 row contains "Done"
  </acceptance_criteria>
  <done>BL-029 verified as already fixed, stale TODO removed, regression test added, backlog item closed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries -- existing transfer endpoint unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | N/A | transfer.py cleanup | accept | Removing a comment, no logic change; regression test confirms correctness |
</threat_model>

<verification>
1. `cd backend && uv run pytest tests/routers/test_transfers.py -x -v` passes
2. `grep -n "TODO" backend/app/services/transfer.py` shows no N+1-related TODO
3. `grep "BL-029" BACKLOG.md` shows "Done"
</verification>

<success_criteria>
BL-029 is verified as already fixed. The stale TODO comment is removed. A regression test exists that verifies the 4-way JOIN loads both transfer legs correctly. The backlog item is marked Done.
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilization/01-02-SUMMARY.md`
</output>
